<UserControl x:Class="Videothek.Terminal.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:VMs="clr-namespace:Videothek.Terminal.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance VMs:LibraryViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView ItemsSource="{Binding VideoLibrary, UpdateSourceTrigger=PropertyChanged , Mode=TwoWay}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="200" Header="Name" DisplayMemberBinding="{Binding Path = Name}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>