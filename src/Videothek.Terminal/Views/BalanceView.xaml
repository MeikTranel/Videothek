<UserControl x:Class="Videothek.Terminal.Views.BalanceViewModel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:viewModels="clr-namespace:Videothek.Terminal.ViewModels"       
      xmlns:local="clr-namespace:Videothek.Terminal.Views"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance viewModels:BalanceViewModel}"       
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="IBANGrid" Grid.Row="0">
            <StackPanel>
                <Label Content="IBAN:" />
                <TextBox x:Name="txtIBAN"  />
            </StackPanel>
        </Grid>
        <Grid x:Name="BetragGrid" Grid.Row="1">

        </Grid>
        <Grid x:Name="AcceptGrid" Grid.Row="2">
            
        </Grid>



    </Grid>
</UserControl>
